configEnvs:
  CONFIG_A: prod CONFIG_A
  CONFIG_B: prod CONFIG_B

httpServer:
  minReplicas: 2
  maxReplicas: 3

  ingress:
    annotations:
      kubernetes.io/ingress.class: nginx
      kubernetes.io/tls-acme: "true"
      cert-manager.io/cluster-issuer: letsencrypt-prod
    host: goapp-prod.gomod.damlys.dev
