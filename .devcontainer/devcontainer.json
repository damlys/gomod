{
  "image": "ghcr.io/damlys/godev:2022-09-06-09-42-11-e64ce98",
  "runArgs": [
    "--add-host=kubernetes.docker.internal:host-gateway"
  ],
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind",
    "source=${localEnv:HOME}/.docker,target=/root/.docker,type=bind",
    "source=${localEnv:HOME}/.kube/config,target=/root/.kube/config,type=bind",
    "source=${localEnv:HOME}/.config/gcloud,target=/root/.config/gcloud,type=bind"
  ],

  "customizations": {
    "vscode": {
      "extensions": [
        "EditorConfig.EditorConfig",
        "golang.go",
        "googlecloudtools.cloudcode",
        "ms-azuretools.vscode-docker",
        "ms-kubernetes-tools.vscode-kubernetes-tools"
      ],
      "settings": {}
    }
  }
}
